<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.robottitto.tarefapmdm03">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.order.PendingOrdersActivity"></activity>
        <activity android:name=".ui.order.RejectedOrdersActivity" />
        <activity android:name=".ui.order.AcceptedOrdersActivity" />
        <activity android:name=".ui.user.RegisterActivity" />
        <activity android:name=".ui.order.PurchasesMadeActivity" />
        <activity android:name=".ui.order.OrdersInProcessActivity" />
        <activity android:name=".ui.order.SelectAddressActivity" />
        <activity android:name=".ui.order.MakeOrderActivity" />
        <activity android:name=".ui.user.ProfileActivity" />
        <activity android:name=".ui.user.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>